{# empty Twig template #}
{% extends "layout.html.twig" %}
{% set drugMenu = true %}

{% block title %}{{ practicien.name }}{% endblock %}

{% block content %}
<h2 class="text-center">{{ block('title') }}</h2>
<div class="well">
    <form class="form-horizontal" role="form">
        <div class="form-group">
            <label class="col-sm-3 control-label">Famille</label>
            <div class="col-sm-9">
                <p class="form-control-static">{{ practicien.family.name }}</p>
            </div>
        </div>
        <div class="form-group">
            <label class="col-sm-3 control-label">Nom légal</label>
            <div class="col-sm-9">
            <p class="form-control-static">{{ drug.name }}</p>
            </div>
        </div>
        <div class="form-group">
            <label class="col-sm-3 control-label">Prenom </label>
            <div class="col-sm-9">
                <p class="form-control-static">{{ drug.first_name }}</p>
            </div>
        </div>
        <div class="form-group">
            <label class="col-sm-3 control-label">Adresse</label>
            <div class="col-sm-9">
                <p class="form-control-static">{{ drug.address }}</p>
            </div>
        </div>
        <div class="form-group">
            <label class="col-sm-3 control-label">Zip Code</label>
            <div class="col-sm-9">
                <p class="form-control-static">{{ drug.zip_code }}</p>
            </div>
        </div>
        <div class="form-group">
            <label class="col-sm-3 control-label">Ville</label>
            <div class="col-sm-9">
                <p class="form-control-static">{{ drug.city }}</p>
            </div>
        </div>
            <div class="form-group">
            <label class="col-sm-3 control-label">Coefficient</label>
            <div class="col-sm-9">
                <p class="form-control-static">{{ drug.notoriety_coefficient }}</p>
            </div>
        </div>
    </form>
</div>
{% endblock %}