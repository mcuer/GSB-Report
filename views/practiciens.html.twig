{# empty Twig template #}
{% extends "layout.html.twig" %}
{% set drugMenu = true %}

{% block title %}Practiciens{% endblock %}

{% block content %}
<h2 class="text-center">{{ block('title') }}</h2>
{% if practiciens %}
    <div class="table-responsive">
        <table class="table table-hover table-condensed">
            <thead>
                <tr>
                    <th>Nom commercial</th>
                    
                </tr>
            </thead>
            {% for practicien in practiciens %}
                <tr>
                    <td><a href="/practiciens/{{ practicien.id }}">{{ practicien.name }}</a></td>
                </tr>
            {% endfor %}
        </table>
    </div>
{% else %}
    <div class="alert alert-warning">Aucun practiciens trouvé.</div>
{% endif %}   
{% endblock %}
